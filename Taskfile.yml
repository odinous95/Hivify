version: '3'

tasks:

  # ============================================================
  # Houses
  # ============================================================

  add-house-migration:
    desc: Create EF Core migration for Houses
    cmds:
      - dotnet ef migrations add {{.migrationName | default "InitialCreate"}} --context HouseDbContext --project src/Houses/Infrastructure/Houses.Infrastructure.csproj --startup-project Hivify.Api/Hivify.Api.csproj

  migrate-house:
    desc: Apply EF Core migrations for Houses
    cmds:
      - dotnet ef database update --context HouseDbContext --project src/Houses/Infrastructure/Houses.Infrastructure.csproj --startup-project Hivify.Api/Hivify.Api.csproj


  # ============================================================
  # Association
  # ============================================================

  add-association-migration:
    desc: Create EF Core migration for Association
    cmds:
      - dotnet ef migrations add {{.migrationName | default "InitialCreate"}} --context AssociationDbContext --project src/Association/Infrastructure/Association.Infrastructure.csproj --startup-project Hivify.Api/Hivify.Api.csproj

  migrate-association:
    desc: Apply EF Core migrations for Association
    cmds:
      - dotnet ef database update --context AssociationDbContext --project src/Association/Infrastructure/Association.Infrastructure.csproj --startup-project Hivify.Api/Hivify.Api.csproj


  # ============================================================
  # Feeds
  # ============================================================

  add-feed-migration:
    desc: Create EF Core migration for Feeds
    cmds:
      - dotnet ef migrations add {{.migrationName | default "InitialCreate"}} --context FeedDbContext --project src/Feeds/Infrastructure/Feeds.Infrastructure.csproj --startup-project Hivify.Api/Hivify.Api.csproj

  migrate-feed:
    desc: Apply EF Core migrations for Feeds
    cmds:
      - dotnet ef database update --context FeedDbContext --project src/Feeds/Infrastructure/Feeds.Infrastructure.csproj --startup-project Hivify.Api/Hivify.Api.csproj


  # ============================================================
  # Complaints
  # ============================================================

  add-complaint-migration:
    desc: Create EF Core migration for Complaints
    cmds:
      - dotnet ef migrations add {{.migrationName | default "InitialCreate"}} --context ComplaintDbContext --project src/Complaints/Infrastructure/Complaints.Infrastructure.csproj --startup-project Hivify.Api/Hivify.Api.csproj

  migrate-complaint:
    desc: Apply EF Core migrations for Complaints
    cmds:
      - dotnet ef database update --context ComplaintDbContext --project src/Complaints/Infrastructure/Complaints.Infrastructure.csproj --startup-project Hivify.Api/Hivify.Api.csproj


  # ============================================================
  # User Management
  # ============================================================

  add-user-migration:
    desc: Create EF Core migration for User Management
    cmds:
      - dotnet ef migrations add {{.migrationName | default "InitialCreate"}} --context UserManagementDbContext --project src/UserMgmt/UserMgmt.Infrastructure/UserMgmt.Infrastructure.csproj --startup-project Hivify.Api/Hivify.Api.csproj

  migrate-user:
    desc: Apply EF Core migrations for User Management
    cmds:
      - dotnet ef database update --context UserManagementDbContext --project src/UserMgmt/UserMgmt.Infrastructure/UserMgmt.Infrastructure.csproj --startup-project Hivify.Api/Hivify.Api.csproj


  # ============================================================
  # All migrations
  # ============================================================

  migrate-all:
    desc: Apply migrations for all bounded contexts
    deps:
      - migrate-house
      - migrate-association
      - migrate-feed
      - migrate-complaint
      - migrate-user